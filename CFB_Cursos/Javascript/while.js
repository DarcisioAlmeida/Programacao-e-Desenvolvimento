

// Comparando parametros For / While / Do while

/* Estrutura For
    for (p1;p2;p3){
        mensagem;
    }
    Estrutura While
    p1;
    while(p2){
        mensagem;
        p3;
    }
*/

// Comparando parametros For / While / Do while
    var num;
    num= 0 ;
    // em vetores e matrizes preferencia para for
    for(num=0; num<10; num++){
        console.log(num+ "\n");
    }
    // quando não souber a quantidade de execuções
    // teste no inicio
    while(num <10){
        console.log(num+ "\n");
        num++;
    }
