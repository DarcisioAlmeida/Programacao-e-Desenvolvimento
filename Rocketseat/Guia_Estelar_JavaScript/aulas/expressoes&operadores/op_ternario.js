// Operador Condicional Ternario

// Dependendo da condição, nós receberemos dois valores diferentes

// Condição então valor 1 se não valor 2 

// condition ? value1 : value2

// Exemplos

// Café da manhã
    let pao = true
    let queijo =  true

cont niceBreakfast = pao && queijo ? 'Café top' : 'Café Ruim'

console.log (niceBreakfast)

// Maior que 18 

let age = 16
const canDrive = age >= 18 ? 'cant drive' : "can't drive"
console.log(canDrive)