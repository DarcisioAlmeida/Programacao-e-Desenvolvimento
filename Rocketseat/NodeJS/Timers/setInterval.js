// setInterval i´ra rodar uma função X (varias vezes) depois de tantos x milisssegundos
// Intervalo de tempo que roda a função

const timeOut = 1000
const checking = () => console.log('checking!')

setInterval(checking, timeOut)


// terminal - node setInterval.js